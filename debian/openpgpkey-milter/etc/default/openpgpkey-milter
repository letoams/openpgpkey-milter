# Defaults for openpgpkey-milter initscript
# sourced by /etc/init.d/openpgpkey-milter
# installed at /etc/default/openpgpkey-milter by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS="--rrtype 65280 --spool /var/spool/openpgpkey-milter --port 8890"
